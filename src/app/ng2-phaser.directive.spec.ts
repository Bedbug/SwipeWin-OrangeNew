import { Ng2PhaserDirective } from './ng2-phaser.directive';

describe('Ng2PhaserDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng2PhaserDirective();
    expect(directive).toBeTruthy();
  });
});
